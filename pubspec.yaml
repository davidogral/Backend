name: gasense_backend
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  postgres: ^2.6.3
  crypto: ^3.0.0

dev_dependencies:
  http: ^1.1.0
  lints: ^5.0.0
  test: ^1.24.0

